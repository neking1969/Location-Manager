version: '3.8'

services:
  app:
    build: .
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=production
      - PORT=5001
    volumes:
      - app-data:/app/server/data
      - app-uploads:/app/server/uploads
    restart: unless-stopped

volumes:
  app-data:
  app-uploads:
